// Seed data for a brand-new business instance of this CRM template.
//
// Kept empty by default — a fresh business should start with zero contacts.
// If you want to see the CRM populated for a demo/preview, import and use
// DEMO_CONTACTS from ./demoContacts.js instead (clearly fictional data only —
// never commit real customer/lead data into this file, since it may end up
// bundled into your production build).

export const initContacts = [];
